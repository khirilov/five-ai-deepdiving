# Довідник доповідача — все, що треба знати до завтрашнього показу

> Прочитай цей файл повністю один раз, потім тримай відкритим розділ 4 (сценарій) і
> розділ 6 (шпаргалка) під час доповіді. Налаштування машини — окремо в
> [setup-guide.md](setup-guide.md), тут — знання і сценарій.

---

## 1. Як влаштований цей репозиторій

Дві половини, які навмисно живуть разом:

- **`src/`** — React-апка, яка і Є презентація (35 слайдів, `pnpm dev` → localhost:5173,
  навігація ←/→/Space, диплінк `#/N`).
- **`.claude/`** — справжні скіли, команди, агенти і хуки, про які розповідають слайди.
  Кожен код-приклад на слайді — цитата реального файлу звідси.

Головний прийом доповіді: **слайд → відкрив файл → запустив**. Ти ніколи не показуєш
«як могло б бути» — тільки те, що реально працює в цьому репо.

Що ще прочитати в репо перед доповіддю (15 хв):
1. `AGENTS.md` — правила репо (їх перевіряє `/deck-review` і `/mr-reviewer`).
2. `.claude/README.md` — мапа демо англійською (можеш показати глядачам).
3. Файли, які цитують слайди: `grill-me/SKILL.md`, `deck-stats/SKILL.md`,
   `deck-review.md` (description), `slide-reviewer.md`, `tdd/SKILL.md` (frontmatter),
   `settings.json`, `.mcp.json`.

---

## 2. Теорія по блоках — глибше, ніж на слайдах

### 2.1 Контекст і токени (фундамент усієї доповіді)

- Контекст-вікно — все, що модель «бачить» у момент відповіді: системний промпт,
  визначення інструментів, CLAUDE.md, історія розмови, результати інструментів.
- Все це **перечитується кожним запитом**. Тому роздутий контекст = повільніше,
  дорожче і — головне — **тупіше**: увага моделі розмазується.
- Базовий податок: системний промпт + вбудовані тули ≈ 20–25k токенів. Кожен MCP-сервер
  додає свої схеми: Chrome DevTools ≈ 18k, Playwright ≈ 13.7k — у КОЖНОМУ запиті.
- Звідси теза доповіді: скіли/агенти/хуки — це три способи давати Claude знання і
  можливості, **не роздуваючи контекст**.

### 2.2 Скіли

**Що це:** папка з `SKILL.md` (frontmatter + markdown-інструкція). Живе в
`.claude/skills/` (проєкт) або `~/.claude/skills/` (особисте, всі проєкти).

**Ключова механіка — прогресивне розкриття:** у контексті постійно висить лише
`description` (~100 токенів на скіл). Повне тіло читається тільки при активації.
Допоміжні файли поруч (як `tdd/tests.md`) — ще один рівень: читаються, лише коли
Claude дійде до відповідного кроку. Порівняй: CLAUDE.md вантажиться весь і завжди.

**Хто запускає:** ти (`/name`) або Claude сам — за `description`. Тому description
пишемо «наполегливо»: *що робить + "Use when: ..." з конкретними тригерами*.
Claude радше недовикористовує скіли, ніж зловживає — опис має «продавати».
Ліміт: description + when_to_use разом ≤ 1536 символів у листингу.

**Команди = скіли.** `.claude/commands/x.md` і `.claude/skills/x/SKILL.md` обидва
створюють `/x` і працюють однаково. Commands — старіший формат (один файл), skills —
новіший (папка + допоміжні файли + повний frontmatter).

**Frontmatter, який треба вміти пояснити:**

| Поле | Суть | Наш живий приклад |
| --- | --- | --- |
| `description` / `when_to_use` | за ним Claude вирішує активувати | усі скіли |
| `argument-hint` | підказка в автокомпліті | feature-forge |
| `arguments` | іменовані аргументи → `$name` у тілі | — (показати на слайді) |
| `disable-model-invocation: true` | тільки руками через /name (деплої!) | **feature-forge / feature-forge-solo** |
| `user-invocable: false` | прихований з /-меню, фонові знання | слайд-приклад |
| `allowed-tools` | тули без запиту дозволу, поки скіл активний | deck-stats |
| `disallowed-tools` | тули ЗАБРАНІ, поки скіл активний | — |
| `model` / `effort` | перемкнути модель/зусилля на час скіла | — |
| `context: fork` + `agent` | тіло скіла виконується у форку-субагенті | **deck-stats** |
| `paths` | автоактивація тільки для файлів за глобом | — |
| `hooks` | хуки, що живуть лише поки скіл активний | **tdd** |

**Підстановки в тілі:** `$ARGUMENTS` (усе після /name), `$0`/`$1` (за позицією),
`$name` (з `arguments:`), `${CLAUDE_SKILL_DIR}` (папка скіла), `` !`команда` `` —
команда виконується, її вивід вставляється в тіло (динамічний контекст).

### 2.3 Субагенти

**Що це:** окремий «працівник» із власним контекст-вікном, власним системним промптом
і обмеженими тулами. Файл `.claude/agents/<name>.md`. Отримує промпт → працює →
повертає ОДИН звіт. Усе сміття дослідження лишається в його вікні.

**Навіщо:** число для сцени — дослідження по 50 файлах ≈ 40k токенів, звіт із нього
≈ 500. Без субагента ці 40k осіли б у твоїй розмові назавжди.

**Поля frontmatter:** `name`, `description` (обов'язкові; за description Claude
делегує сам), `tools`/`disallowedTools` (read-only агент = безпечний агент), `model`
(haiku для рутини — наш slide-reviewer), `permissionMode`, `maxTurns` (стоп-кран),
`skills` (скіли, ПОВНІСТЮ влиті в контекст агента на старті), `memory`
(user/project/local — агент пише собі нотатки між сесіями), `isolation: worktree`
(окрема копія репо), `background`.

**Вбудовані:** Explore (read-only пошук), Plan (архітектор), general-purpose.

**Скіл чи агент? Питання — інтерактивність.** Субагент НЕ може говорити з користувачем
посеред роботи. Тому все, що інтерв'ює (grill-me сесії: mr-reviewer, feature-forge,
deck-review) — команди в головній розмові; все автономне read-only (slide-reviewer,
design-explorer, mantine-воркери) — субагенти. Це наш власний урок міграції з Copilot —
розказано в `.claude/README.md`.

**Патерн fan-out / fan-in:** оркестратор → N паралельних воркерів → (опційно)
reconciler зливає звіти → один результат. У нас два приклади: `/deck-review`
(живий, у цьому репо) і `/mantine-css-auditor` (з робочого проєкту, розповісти).

### 2.4 Хуки

**Головна теза:** промпт — прохання, хук — гарантія. «Запускай тести» в CLAUDE.md
Claude може забути; хук на PostToolUse не забуде ніколи, бо це не рішення моделі,
а код, який виконує сам Claude Code.

**Як влаштовано:** `settings.json` → подія → matcher (ім'я тула: `Bash`, `Edit|Write`,
regex `mcp__.*`) → список хуків. Типи: `command` (скрипт), `prompt` (спитати модель
так/ні), `agent` (спавнить субагента-перевіряльника), `http`, `mcp_tool`.

**Контракт command-хука:** на stdin приходить JSON (session_id, tool_input...).
Exit code `0` — ок (stdout може містити JSON-рішення); exit code `2` — блок, stderr
повертається Claude як фідбек — він піде виправляти. PreToolUse може повернути
`permissionDecision: allow | deny | ask`.

**Події (30+), головні для розповіді:** SessionStart, UserPromptSubmit,
PreToolUse (може блокувати), PostToolUse, PermissionRequest, Stop, SubagentStart/Stop,
PreCompact, SessionEnd. Перегляд: `/hooks` (read-only список з джерелами).

**Рівні:** глобальні (`~/.claude/settings.json`), проєктні (`.claude/settings.json` —
комітяться: правила команди), локальні (`settings.local.json`), і **скоуповані** —
у frontmatter скіла чи агента: діють лише поки той активний (наш tdd).

**Наші три хуки** (`.claude/settings.json`): PostToolUse Edit|Write → typecheck
(async, статус у барі); PreToolUse Bash → `block-git-push.mjs` (deny на git push);
Stop → звуковий сигнал. Важливо: хуки читаються на старті сесії — після правки
settings.json потрібен рестарт сесії.

### 2.5 Токени: практики економії

1. Діагностика перш за все: `/context` — розклад вікна по категоріях.
2. `/clear` між незалежними задачами — найнедооціненіша команда.
3. `/compact` свідомо на довгих сесіях (авто-microcompact теж є, але керований кращий).
4. Дешеві моделі під рутину: `model: haiku` у субагентах (+ `maxTurns`).
   На рівні сесії — `/model`: `default` (Opus → Sonnet після ~50% usage),
   **`opusplan`** (Opus думає в Plan Mode → Sonnet пише код — розумна модель лише
   там, де платить), або `sonnet`/`haiku` напряму. Слайд про це — Tokens (#29).
5. `effort` — глибина «думання» і витрати: `low` (субагенти/рутина) · `medium`
   (прості, cost-sensitive) · `high` (дефолт Claude Code, більшість роботи) ·
   `xhigh` (кодинг/агентика) · `max` (correctness > cost, може перемудрити).
   Ставиться у frontmatter скіла/агента (`effort: low`) або глобально.
6. Вимкнути невикористовувані MCP (найбільший прихований податок).
7. Процедури з CLAUDE.md → у скіли (description висить, тіло — на вимогу).
8. **caveman** (плагін): ріже балачки в output, −65% у середньому (розкид 22–87%),
   код ідентичний, thinking не чіпає. Режими lite/full/ultra. Чесна примітка
   на сцені: це економія на видимих відповідях, не на «мисленні».
9. Моніторинг: `/usage`, зовнішній `ccusage`.

### 2.6 MCP

**Що це:** відкритий протокол «USB-порт для AI»: сервер оголошує тули, Claude бачить
їх як `mcp__<сервер>__<тул>`. Транспорти: stdio (локальний процес) і http (віддалений).

**Скоупи і пріоритет:** local (тільки я, цей проєкт; `~/.claude.json`) → project
(`.mcp.json` у корені, комітиться — вся команда) → user (всі мої проєкти).
При дублюванні імен виграє local > project > user.

**Безпека/ціна (точна механіка для питань із залу):** проєктні сервери вимагають
підтвердження при першому запуску (захист від чужого конфігу). Числа типу «DevTools
≈ 18k» — це розмір СХЕМ тулів, а не «спалюється при кожному використанні»:
1) схеми завантажуються в контекст і, оскільки модель stateless, надсилаються з
кожним запитом — але через prompt caching цей префікс кешується (cache read ≈ в 10
разів дешевше), тож платиш переважно МІСЦЕМ у вікні та «увагою» моделі, а не повною
ціною токенів щоразу; 2) новіші версії Claude Code взагалі ВІДКЛАДАЮТЬ завантаження
MCP-схем (tool search): поки тул не потрібен, у контексті лише імена — повна схема
підтягується при першому використанні; 3) сам виклик тула коштує окремо: виклик +
результат (снепшот сторінки від Playwright може бути кілька тисяч токенів). Висновок
незмінний: реальну вагу дивись у `/context`, зайве вимикай. Тули, що читають
зовнішній контент (GitHub issues/PR!) — вектор prompt injection: користуйся на об'єктах,
яким довіряєш.

**Наш `.mcp.json`:** chrome-devtools + playwright + context7 (без секретів; ключі —
тільки в local scope).

**Різниця DevTools vs Playwright (топ-питання із залу):** DevTools = СПОСТЕРІГАТИ
(консоль, network, перф-трейси, Lighthouse; CDP, тільки Chromium; ~26 тулів ≈ 18k
токенів) — питання «чому повільно/чому помилки». Playwright = ДІЯТИ (драйв юзер-флоу
через accessibility-снепшоти; крос-браузер; ~21 тул ≈ 13.7k) — питання «чи працює
флоу e2e». Не конкуренти. Обидва разом постійно = ~32k податку — вмикай під задачу.

**Figma Dev Mode** (нема доступу — слайд): desktop app + Dev/Full seat (Professional+),
Preferences → Enable Dev Mode MCP Server → `http://127.0.0.1:3845/mcp`; Claude читає
реальну структуру макета (auto-layout, варіанти, токени), не скріншот.

**GitHub:** MCP **не потрібен** — `gh` CLI (вже стоїть) покриває все. Офіційний
`api.githubcopilot.com/mcp` через `claude mcp add` дав `Incompatible auth server: does
not support dynamic client registration` (OAuth DCR не підтримується), тож пішли через
`gh`. Зв'язка з нашим `/github-pr-summary`: згенерував опис → `gh pr create --draft` без
виходу з термінала. Це й є живий приклад тези «не вмикай MCP, якщо CLI вистачає». (Якщо
MCP таки треба — PAT у заголовку Authorization, токен у local config.)

**Perplexity (Sonar):** пошук, що повертає готову відповідь з цитатами замість тонн
HTML → економія токенів. Потрібен платний API-ключ → local scope.

### 2.7 Плагіни і Learning mode

**Плагін** = формат дистрибуції: пакет «скіли + агенти + хуки + MCP», що ставиться
однією командою глобально (`~/.claude/plugins/`). Маркетплейс = git-репо зі списком
пакетів. Команди: `claude plugin marketplace add <owner/repo>`,
`claude plugin install <plugin>@<marketplace>`, меню `/plugin`.
Каталог: claude.com/plugins (офіційний маркетплейс підключений за замовчуванням).

Імена для сцени: **frontend-design** (Anthropic, ~830k встановлень — виразний UI
замість «AI-слопу»), **caveman** (токени), **typescript-lsp** (діагностика типів як
в IDE), **context7** (той самий MCP, але плагіном), **superpowers** (~750k — приклад
масштабу екосистеми).

**Learning mode** (бонус-слайд): НЕ MCP — режим спілкування (output style), тепер і
офіційний плагін `learning-output-style`. Під капотом це просто інструкції, додані до
системного промпта (плагін робить це SessionStart-хуком) — ще один доказ тези «все
розширення Claude = текст у правильному місці».

- **Learning**: Claude пише обв'язку сам, але на змістовних рішеннях (бізнес-логіка з
  кількома валідними підходами, обробка помилок, вибір алгоритму) зупиняється, готує
  місце і лишає `TODO(human)` — «напиши ці 5–10 рядків сам», пояснивши трейдофи.
  Очевидне/boilerplate/CRUD він все одно пише сам — ти працюєш тільки над тим, що вчить.
- **Explanatory**: Claude працює як зазвичай, але кожне неочевидне рішення супроводжує
  блоком `★ Insight` з 2–3 пунктами «чому так».

**Як виглядає сесія в Learning:** просиш фічу → Claude скаффолдить і каже «у файлі X
є TODO(human), ось контекст і два можливі підходи» → ти пишеш свій шматок у файлі →
кажеш «готово» → Claude рев'юїть твій код і рухається далі.

**Вмикання (актуально для CLI ≥ 2.1.91):** `/config` → **Output style** → Learning /
Explanatory (є ще Proactive). Вибір зберігається в `.claude/settings.local.json` як
`"outputStyle": "Learning"` — можна виставити й руками. ⚠️ Системний промпт читається
раз на старті сесії, тому стиль вмикається **після `/clear` або нової сесії** — на
демо переключи заздалегідь! Стара команда `/output-style` видалена у v2.1.91 — не
показуй її. Альтернатива для дистрибуції на команду: плагін
`learning-output-style@claude-plugins-official` (він уже встановлений на цій машині).
**Вимикання:** `/config` → Output style → Default — інструкції режиму додаються в
системний промпт кожної сесії, тож поза навчанням це зайві токени.

Цикл «вивчи будь-що»: маленький проєкт → learning mode → ти заповнюєш пропуски →
`/grill-me` по написаному → повторити.

---

## 3. Файл за файлом: що в нашому `.claude` і що в кожному показувати

| Файл | Що це | Що показати пальцем |
| --- | --- | --- |
| `skills/grill-me/SKILL.md` | мінімальний скіл (9 рядків) | весь файл — «скіл це просто markdown» |
| `skills/tdd/SKILL.md` + 5 .md поруч | скіл з допоміжними файлами | frontmatter з `hooks:`; список файлів = прогресивне розкриття |
| `skills/deck-stats/SKILL.md` | скіл у форку | `context: fork` + `agent: Explore`, `allowed-tools`, `$ARGUMENTS` |
| `skills/design-an-interface/` | «design it twice» | крок зі спавном 3+ субагентів |
| `skills/feature-forge/SKILL.md` | оркестратор фаз (субагенти в DESIGN) | `disable-model-invocation: true` + `$ARGUMENTS`: набираєш `/feature-forge <фіча>`, сам не тригериться |
| `skills/feature-forge-solo/SKILL.md` | той самий процес БЕЗ субагентів | окремий скіл замість флага; різниця — лише Phase 2 |
| `skills/learn-with-me/SKILL.md` | скіл-коуч навчання (бонус-демо) | фази SCOPE→PLAN→LEARN→QUIZ→REVIEW; план у `docs/learning-plans/`; `disable-model-invocation` + `$ARGUMENTS`; зв'язка з Learning style |
| `commands/deck-review.md` | швидкий fan-out | Modes; крок «spawn one slide-reviewer per section» |
| `commands/mr-reviewer.md` | автоматичне рев'ю всіх змін | Step 1: сам збирає staged+unstaged+untracked, не питає що рев'юїти |
| `commands/browser-reviewer.md` | QA у браузері | вимога chrome-devtools MCP у шапці |
| `commands/mantine-css-auditor.md` | fan-out з роботи | кроки worker × N → reconciler |
| `agents/slide-reviewer.md` | дешевий воркер | `model: haiku`, `maxTurns: 8`, read-only tools |
| `agents/design-explorer.md` | дизайн-воркер | «не згладжуй до середини — цінність у крайнощах» |
| `agents/mantine-css-audit-*.md` | воркер + reconciler | consensus-правила reconciler-а |
| `settings.json` | 3 проєктні хуки | matcher, async, шлях до block-git-push.mjs |
| `hooks/block-git-push.mjs` | PreToolUse гейт | 30 рядків: stdin JSON → deny |
| `.mcp.json` (корінь) | project-scope MCP | три сервери, нуль секретів |
| `CLAUDE.md` (корінь) | пам'ять проєкту | один рядок `@AGENTS.md` — імпорт |

---

## 4. Сценарій: слайд ↔ демо (з запасними варіантами)

> **Дека = 43 слайди** (оновлено: додано 8 нових — позначені 🆕). Нумерація `#/N` нижче точна.

| Слайди | Блок | Демо | Команди |
| --- | --- | --- | --- |
| 1–2 | Інтро: title + recap | — | — |
| 🆕 3 | «навіщо ця доповідь» (не *що*, а *як правильно*) | проговорити тезу дня | — |
| 4 | план | — | — |
| 5–11 | Скіли: теорія (what → anatomy → trigger → frontmatter 1-3 → substitutions) | відкрити `grill-me/SKILL.md` поруч зі слайдом анатомії | — |
| 12 | /deck-stats | ⭐ ЖИВЕ: скіл у форку | `/context` → `/deck-stats` → `/context` — вікно чисте |
| 13 | feature-forge | відкрити обидва SKILL.md, пройтись по фазах; живий запуск DISCOVER+DESIGN (§8) | `/feature-forge talk timer...` |
| 🆕 14 | коли писати скіл / коли ні (facts→CLAUDE.md, procedures→skills…) | — | — |
| 🆕 15 | правила скілів: «що написати → яку поведінку купуєш» | можна тут спробувати зрізати кут → скіл відмовить (§7) | — |
| 16–21 | Субагенти: теорія + порівняння | відкрити `slide-reviewer.md` (haiku); на слайді порівняння — ⭐ `/deck-review` (поки працює — розповідати) → `/context`; якщо встигаєш `--solo` | `/deck-review` |
| 22–23 | Хуки: теорія | — | — |
| 24 | хуки репо | ⭐ ЖИВЕ: `/hooks`; попросити `git push` → deny; звук на Stop | `/hooks` |
| 25–26 | хук у скілі (tdd) | показати frontmatter tdd | — |
| 27–28 | Токени: куди йдуть + вбудована механіка | ⭐ `/context` наживо (видно MCP-податок!) | `/context` |
| 🆕 29 | model & effort як важелі ціни (opusplan, effort-рівні) | проговорити `/model opusplan` | `/model` |
| 30 | caveman | ⭐ одне питання до/після | `/caveman full` |
| 31 | чек-лист економії | — | — |
| 32–33 | MCP: огляд + `.mcp.json` | показати файл + `/mcp` | `/mcp` |
| 🆕 34 | коли MCP / коли НЕ MCP (default = off; gh замість github MCP) | — | — |
| 🆕 35 | детальна ціна запиту з MCP (схеми/кеш/deferred/виклик) | ⭐ звʼязати з `/context`: «оце і є та вага» | `/context` |
| 36 | DevTools vs Playwright | ⭐ опційно: `/browser-reviewer` АБО «open localhost:5173 and check console» (потрібен Node ≥ 20.19, див. setup §2.1) | — |
| 37–38 | Figma, Perplexity | розповідь | — |
| 🆕 39 | плагіни як механізм (frontend-design, typescript-lsp, superpowers) | ⭐ опційно: `/plugin` (меню) або frontend-design до/після | `/plugin` |
| 40 | старт завтра | — | — |
| 41 | learning machine | ⭐ опційно: `/config`→Output style→Learning + дрібна фіча → TODO(human) | `/config` |
| 🆕 42 | learn-anything loop (як саме вчитись) | — | — |
| 43 | Questions | — | — |

**Бюджет часу (40 хв):** інтро 3 + скіли 10 (з deck-stats) + агенти 8 (з deck-review) +
хуки 6 + токени 5 + MCP 5 + фінал 3. Нові слайди (3, 14, 15, 29, 34, 35, 39, 42) — короткі
концептуальні, по ~30–45 с кожен; за потреби кілька можна прогорнути швидко.
**Якщо горить час, ріжеться в такому порядку:** browser-reviewer (слайд 36) → learning
live (41, лишити розповідь) → caveman live (30, лишити слайд) → deck-review --solo
(показати тільки дефолтний) → plugins live (39, лишити слайд).
**Запасне демо:** якщо deck-review піде не так — `/mr-reviewer` на незакомічених
змінах (він сам збере diff) теж виглядає ефектно і займає ~хвилину.

**⌨️ Введення команд з аргументами на сцені:** Enter на підказці автокомпліту =
ВІДПРАВИТИ одразу (з порожніми аргументами). Щоб дописати аргумент: **Tab** приймає
підказку і лишає курсор у полі → пробіл → опис фічі → Enter. Тобто:
`/feat` → Tab → `кошик з фільтрами` → Enter. Якщо випадково відправив порожнім —
не страшно: обидва forge-скіли спершу спитають «What feature are we building?» і
чекатимуть відповідь.

---

## 5. Ймовірні питання із залу (і відповіді)

- **«Чим скіл відрізняється від CLAUDE.md?»** — CLAUDE.md вантажиться весь і завжди
  (факти, конвенції). Скіл — на вимогу (процедури). Правило: якщо шматок CLAUDE.md
  став покроковою інструкцією — виноси в скіл.
- **«Скіл vs субагент?»** — скіл = ЯК робити (інструкція в поточній розмові),
  субагент = ХТО робить (окремий контекст). Комбінуються: `context: fork` запускає
  скіл у субагенті; поле `skills` вливає скіли в агента.
- **«Чи може субагент спавнити субагентів / спитати мене?»** — ні. Оркеструє головна
  розмова. Тому інтерактивні речі — команди, автономні — агенти.
- **«Хуки — це ж виконання довільного коду?»** — так, тому: тільки свої/перевірені,
  `/hooks` показує що звідки прийшло, проєктні хуки проходять код-рев'ю як код.
- **«Скільки «коштує» MCP-сервер?»** — схеми тулів у кожному запиті: DevTools ≈ 18k,
  Playwright ≈ 13.7k токенів. Показати `/context`.
- **«А якщо два скіли підходять?»** — Claude обирає за description; завжди можна явно
  `/name`. Конфлікти лікуються чіткішими "Use when".
- **«Чи їде наш код кудись через MCP?»** — MCP-сервер ти обираєш сам (локальний процес
  або твій/вендорський endpoint). У модель потрапляє те, що тул повернув у розмову —
  як і будь-який інший результат інструмента.
- **«Як поділитися цим з командою?»** — закомітити `.claude/` і `.mcp.json`. Особисте —
  у `~/.claude/`. Третій рівень — зробити свій плагін-маркетплейс.
- **«Хук може зупинити Claude взагалі?»** — так: PreToolUse deny блокує дію,
  `continue: false` зупиняє хід повністю, exit 2 повертає фідбек на виправлення.
- **«Це працює не тільки з Claude?»** — скіли: формат Agent Skills — відкритий стандарт
  (agentskills.io), MCP — відкритий протокол, який підтримують й інші клієнти.
  Хуки/агенти в цьому вигляді — специфіка Claude Code.

---

## 6. Шпаргалка команд (тримати під рукою)

```bash
# сесія
/help            # список команд і скілів
/context         # розклад контекст-вікна ← головний доказ у доповіді
/clear           # чистий контекст
/compact         # стиснути історію
/hooks           # які хуки звідки
/mcp             # статус MCP, OAuth, список тулів
/plugin          # меню плагінів
/config          # → Output style: Default | Explanatory | Learning | Proactive
/model           # default | opusplan (Opus думає / Sonnet пише) | sonnet | haiku
/usage           # ліміти/використання

# наші демо
/deck-stats                # context: fork (порівняй /context до/після)
/deck-review               # fan-out: 7 × slide-reviewer (haiku)
/deck-review --solo        # те саме інлайн
/feature-forge <фіча>      # повний оркестратор, субагенти в DESIGN (довге)
/feature-forge-solo <фіча> # те саме без субагентів — для порівняння
/grill-me                  # інтерв'ю по плану
/mr-reviewer               # сам збирає ВСІ зміни і рев'юїть
/browser-reviewer          # QA у браузері (потрібен chrome-devtools)
/learn-with-me <тема>      # коуч навчання: грілить → план-файл → веде по 1 темі

# mcp (довідково — у репо вже все в .mcp.json)
claude mcp list
claude mcp add --transport stdio chrome-devtools -- npx chrome-devtools-mcp@latest
claude mcp add --transport http context7 https://mcp.context7.com/mcp

# плагіни (УЖЕ встановлені на цій машині, user scope — перевір: claude plugin list)
claude plugin list                                          # усі мають бути ✔ enabled
claude plugin marketplace add JuliusBrussee/caveman         # маркетплейс caveman
claude plugin install caveman@caveman
claude plugin install frontend-design@claude-code-plugins   # є і в claude-plugins-official
claude plugin install typescript-lsp@claude-plugins-official
claude plugin install learning-output-style@claude-code-plugins

# відкат будь-яких демо-змін у коді
git checkout -- src
```

---

## 7. Демо feature-forge: фічі, сценарій по фазах, уроки для слухачів

### Які фічі тригерити

| Коли | Фіча | Що набирати |
| --- | --- | --- |
| **Репетиція (сьогодні)** | Section jump: клік на назву секції у футері → список секцій → перехід на її перший слайд | `/feature-forge-solo section jump menu in the deck footer` → `/clear` → `/feature-forge section jump menu in the deck footer` |
| **Демо (на доповіді)** | Talk timer: таймер у футері — скільки йде доповідь, ціль 40 хв, підсвітка при перевищенні | `/feature-forge talk timer in the deck footer with a 40-minute target` (через **Tab**!) |

**Чому саме такі фічі:** малі (хук + шматок футера), результат видно на екрані одразу,
користь справжня (таймер реально потрібен тобі на доповіді — гарна іронія для залу),
і головне — **простір дизайну не вироджений**: «мінімальна поверхня» (хук
`useTalkTimer` що повертає рядок), «гнучкість» (компонент `<TalkTimer>` з пропсами
target/warn/format), «оптимізація під найчастіший кейс» (конфіг у деці + нуль API)
дадуть реально різні інтерфейси. На тривіальній фічі субагенти повернули б три
однакові відповіді — і демо здулося б.

Репетиційну фічу (section jump) після прогону або лишай і закоміть (вона корисна),
або відкоти: `git checkout -- src` (тому git init — ДО репетиції).

### Що відбувається по фазах і що ГОВОРИТИ

**Phase 1 — DISCOVER (~3 хв на демо).** Скіл змушує Claude працювати продактом:
структуровані питання через AskUserQuestion, кожне з рекомендованим дефолтом.
Очікувані питання для таймера: стартувати автоматично з першого слайда чи по клавіші?
формат (mm:ss)? що при перевищенні 40 хв (колір/блимання)? де саме у футері?
*Говорити:* «зверни увагу — він не питає того, що може знайти в коді: це правило
прямо в тілі скіла». На демо обирай Recommended-варіанти — швидко. В кінці фази —
спека зберігається у `docs/specs/...-spec.md`. *Говорити:* «контекст смертний, файли
ні: спека переживе /compact і навіть нову сесію».

**Phase 2 — DESIGN (~4–5 хв).** У звичайному режимі: ОДНЕ повідомлення з трьома
паралельними викликами design-explorer — покажи це в транскрипті. Поки агенти
працюють (це ~хвилина тиші — ідеальний момент!), відкрий
`.claude/agents/design-explorer.md` і пройдись: read-only tools (не може нічого
зламати), `model: sonnet` (дешевше), `maxTurns: 15` (стоп-кран), і ключовий рядок
інструкції — *«Do not hedge toward a middle ground — the value of parallel
exploration is in the extremes»*. *Говорити:* «агенти не бачать один одного — тому
нема якоріння на першій ідеї; саме це втрачається в solo-режимі». Коли звіти
повернулись — три різні інтерфейси поруч. Тут запусти `/context`: історія майже не
виросла, бо в неї впали лише три короткі звіти, а не процес їх народження.

**Phase 3 — REVIEW: тут СТОП на демо.** Обери дизайн (або скажи «синтезуй 1+3»),
подивись Implementation Plan — список ПОВЕДІНОК (не кроків), кожна = один TDD-цикл.
*Говорити:* «далі BUILD жене це через tdd-скіл: red-green-refactor по одній поведінці.
Показувати 10 хвилин написання коду не буду — але фази 4–5 працюють так само:
скіл не пустить далі, поки typecheck/тести червоні». Чесна примітка, якщо спитають:
у цьому репо тест-ранер не налаштований, тож BUILD тут спирається на typecheck;
повний TDD-цикл демонструється на проєкті з тестами.

### Уроки «що писати в скілах і для чого» (проговорити по ходу)

Це відповідь на питання слухача «окей, а ЯК мені написати такий скіл собі?» —
кожен пункт: що написано → яку поведінку це купує:

1. **`disable-model-invocation: true`** → скіл не самозапускається. Без цього
   агресивний description («Use when: implementing a new feature...») тригерив
   5-фазний процес на будь-яке «зроби фічу». Дія з великими наслідками = ручний запуск.
2. **`$ARGUMENTS` + guard на порожнє** → можна писати `/feature-forge <опис>` одним
   рядком, а випадковий Enter не ламає демо — скіл просто питає, що будуємо.
3. **Явні фази + «No proceeding without approval»** → без цього модель «пролітає»
   в код на першому ж повідомленні. Текст скіла = поведінкові рейки.
4. **Композиція скілів по імені** (grill-me → design-an-interface → tdd) → маленькі
   скіли перевикористовуються окремо; оркестратор лише тримає процес.
5. **«Якщо відповідь є в кодовій базі — дослідь код, не питай»** → один рядок, який
   прибирає 80% дурних питань в інтерв'ю.
6. **Persist артефактів у файли** (спека в docs/specs) → довгі процеси переживають
   ліміти контексту.
7. **Брифи субагентам: різні обмеження + жорсткий формат звіту + ліміт рядків** →
   паралельні агенти повертають порівнюване, а не три есеї.
8. **Секція Enforcement Rules** → правила, які скіл цитує тобі ж, коли ти намагаєшся
   зрізати кут (спробуй на демо сказати «давай одразу код» — він відмовить).
9. **TodoWrite на старті** → глядач (і ти) бачиш прогрес фаз у тудушках.

### Чек-лист репетиції сьогодні

1. [ ] `git init` + коміт (відкат і diff-демо).
2. [ ] `/feature-forge-solo section jump menu in the deck footer` — пройди DISCOVER
   швидко (Recommended), дочекайся 3 дизайнів інлайн → запиши число з `/context`.
3. [ ] `/clear` → `/feature-forge section jump menu in the deck footer` — та сама
   фіча, ті самі відповіді → порівняй `/context`. Запиши обидва числа — назвеш їх
   на доповіді, навіть якщо живе порівняння не влізе в таймінг.
4. [ ] Виріши долю section jump: лишити й закомітити, або `git checkout -- src`.
5. [ ] Засічи час фаз 1–2 — це твій таймінг для слайда 12/18.
6. [ ] Talk timer НЕ чіпай до доповіді — демо має бути справжнім.

---

## 8. Що почитати додатково (якщо лишиться вечір)

1. Офіційні доки — по одній сторінці на блок доповіді:
   - https://code.claude.com/docs/en/skills (зверни увагу на Frontmatter reference)
   - https://code.claude.com/docs/en/sub-agents (Example subagents внизу)
   - https://code.claude.com/docs/en/hooks (Decision control + події)
   - https://code.claude.com/docs/en/mcp (скоупи, .mcp.json)
   - https://code.claude.com/docs/en/output-styles
2. Наш [setup-guide.md](setup-guide.md) — покрокові налаштування + траблшутінг.
3. `.claude/README.md` — коротка англійська мапа демо (можна лишити відкритою на екрані).
4. Каталоги для «що далі»: https://claude.com/plugins, https://agentskills.io.
